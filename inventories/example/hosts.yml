---
all:
  children:
    physical_ci:
      hosts:
        hil-01:
          # RFC 5737 documentation address. Replace only in an ignored local inventory.
          ansible_host: 192.0.2.10
          ansible_user: bootstrap-admin
