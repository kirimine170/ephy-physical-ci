# Local inventory

This directory is intentionally ignored except for this README and `.gitignore`．Copy the example inventory here and
store host addresses，SSH key paths，authorized keys，password hashes，and full USB serial numbers only in the local copy．

Never commit a local inventory，even when values are encrypted．This repository is public and does not use Git as a
secret store．
